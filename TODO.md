# Parser

- [ ] Fix code generation
  - Currently we have some hard coded code segments in what is suppose to be generated code. Either update the templates to generate them and move them to somewhere else.
- [ ] Add unit tests to validate output of tokenizer as well
- [ ] Add special case parsers
  - [ ] XML parser
  - [ ] Regex parser
  - [ ] Documentation parser
